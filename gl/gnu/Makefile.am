## Makefile for gl/gnu

# gnulib.mk appends to these variables, so we need to set them here.
AM_CFLAGS =
BUILT_SOURCES =
EXTRA_DIST =
MOSTLYCLEANFILES =
noinst_LTLIBRARIES =

include gnulib.mk

AM_CFLAGS += $(GNULIB_WARN_CFLAGS) $(WERROR_CFLAGS)
